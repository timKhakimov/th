const { CohereClient } = require("cohere-ai");

const cohere = new CohereClient({
  token: "AjhhpA3sUkWHmeS3ZmpPECbp2GgqPwSAPzJkVJFi",
});
//XE8gn6U4fFyXkvaQ9Y6zJAonOxisDdrECw0PrN1H

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();

(async () => {
  const { text } = await cohere.chat({
    chatHistory: [
      { role: "USER", message: "Who discovered gravity?" },
      {
        role: "CHATBOT",
        message:
          "The man who is widely credited with discovering gravity is Sir Isaac Newton",
      },
    ],
    model: "command-r-plus",
    message: "What year was he born?",
  });

  console.log(text);
})();